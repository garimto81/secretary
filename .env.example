# Secretary Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# API Keys
# =============================================================================

# Anthropic API key for Claude (required for response drafting)
ANTHROPIC_API_KEY=sk-ant-...

# GitHub personal access token (required for GitHub integration)
# Required scopes: repo, read:user
GITHUB_TOKEN=ghp_...

# =============================================================================
# Optional Configuration
# =============================================================================

# Override default settings via environment variables
# Format: SECRETARY_{SECTION}_{KEY} (uppercase, dots become underscores)

# Example: Enable/disable specific adapters
# SECRETARY_ADAPTERS_GMAIL_ENABLED=true
# SECRETARY_ADAPTERS_SLACK_ENABLED=false

# Example: Set notification analysis threshold
# SECRETARY_ANALYSIS_URGENCY_THRESHOLD=0.7

# =============================================================================
# Paths (Windows)
# =============================================================================

# Google OAuth credentials (default: C:\claude\json\desktop_credentials.json)
# SECRETARY_GOOGLE_CREDENTIALS_PATH=C:\path\to\credentials.json

# Gmail token (default: C:\claude\json\token_gmail.json)
# SECRETARY_GMAIL_TOKEN_PATH=C:\path\to\token.json

# Calendar token (default: C:\claude\json\token_calendar.json)
# SECRETARY_CALENDAR_TOKEN_PATH=C:\path\to\token.json

# Slack credentials (default: C:\claude\json\slack_credentials.json)
# SECRETARY_SLACK_CREDENTIALS_PATH=C:\path\to\credentials.json
