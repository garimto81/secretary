# Google API (Gmail, Calendar)
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0

# HTTP Client (GitHub API)
httpx>=0.25.0

# WebSocket (Android Notification)
websockets>=12.0

# Automation Actions
winotify>=1.1.0  # Windows Toast notifications
anthropic>=0.18.0  # Claude API for response drafting

# MS To Do Integration (Phase 5)
msal>=1.24.0  # Microsoft Authentication Library

# Life Event Management (Phase 5)
korean-lunar-calendar>=0.3.1  # Lunar/Solar calendar conversion

# Gateway Storage (Phase 4)
aiosqlite>=0.19.0  # Async SQLite for message storage

# Web Search (Intelligence)
ddgs>=6.0.0  # DuckDuckGo search (duckduckgo_search renamed to ddgs)
